# PROJECT_STATE

v2 REDESIGN COMPLETE (loopbox-v2-kit, 8 phases on branch claude/eloquent-feynman-4kj3zr). Earlier: P0 complete (m6-green), feature freeze (m9-green).

GOAL: Win NYP Open Hack with a live golden path (play → open → confirm & pay → trade → verify fairness) across three live drops, plus thin B2B and C2C, built entirely by prompting.
DONE (v2): Phase 1 kit images in public/images/themes, themes table + idempotent seed from src/data/themes.seed.json (Naruto 100 and Cyberpunk: Edgerunners 60 as committed-shuffle campaigns), image helper and CharacterImage (robot art kept only for Astral Kin). Phase 2 v2 tokens site-wide and the new asymmetric homepage. Phase 3 /drops browser with tabs, To be continued + Notify me, data-driven drop pages with lightbox and blurred secrets. Phase 4 open-before-pay: order_items state machine, /checkout with decline and countdowns, "Are you sure?" modal, demo payment for concept drops, multi-figure Stripe sessions, anti-abuse tests. Phase 5 OpeningSequence (3D box, foil pack, swipe-to-tear with canvas trail, rarity reveal; skip, keyboard, reduced motion). Phase 6 physical figures by QR (hash-only codes, camera/photo/manual, /claim/[code], /admin/qr-sheet, seed:physical). Phase 7 marketplace storefront (trust strip, URL-synced filters + sort, buyer-first cards, protection box). Phase 8 QA: 173 unit, 12 e2e, Lighthouse a11y 100 on six pages, fake-webcam scan, screenshots in docs/screens. Details: docs/REDESIGN_NOTES.md.
IN PROGRESS: nothing
BLOCKED: nothing
BUGS: none known.
DECISIONS (v2): kit data lives in src/data (data/ is the ignored SQLite folder); Astral Kin keeps Stripe Checkout Sessions + signed webhook (Payment Links can't bind to a reservation, AGENTS §12); opening draws the lowest free pool position (unchanged rule) and holds it, confirmation allocates exactly that unit; per-person max counts slots won; admin QR sheet generates fresh batches because stored codes are hashes only; "Trade it" on an unpaid figure asks to confirm first (trade logic unchanged); camera=(self) in Permissions-Policy; Dockerfile not changed (it doesn't copy public/ — noted, Render unaffected); render.yaml branch switched to this branch at the human's request; protected e2e strings changed where the brief changed the screen (AGENTS §24/§25).
NEXT 3 ACTIONS: 1) redeploy on Render (render.yaml now deploys claude/eloquent-feynman-4kj3zr; an existing Render service created from the old Blueprint may need its branch switched in the dashboard, or re-sync the Blueprint)  2) print a QR sheet from /admin/qr-sheet for the physical demo  3) rehearse the updated DEMO.md on the live URL
LAST GREEN TAG: m9-green (v2 commits are green but untagged)

---

Previous state (before v2), kept for history:

P0 COMPLETE (m6-green). FEATURE FREEZE (m9-green): only fixes and the demo pack from here.

GOAL: Win NYP Open Hack with a redesigned, live golden path (play → Stripe test pay → reveal → same-rarity swap → verify fairness) plus thin B2B and C2C, built entirely by prompting in 48 h.
DONE: Demo pack — DEMO.md (90-second golden path with exact labels, marketplace add-on, backups A–E, pre-demo checklist), PITCH.md (5 minutes, hypothetical hook labelled, no invented statistics, business numbers labelled assumptions), QA.md (30 judge questions with honest answers), docs/screenshots/ (one per step from the e2e run); marketplace pending-order button now reads "Simulate payment (demo)"; wording pass so only the two spec-mandated phrases use the word "legal" (README heading, /terms heading). M9 hardening — tests/authz.test.ts (every IDOR case in AGENTS.md section 17, every ✗ cell of the RBAC matrix with an endpoint, DEMO_MODE=false smoke); request bodies capped while streaming (readBody: 8 KB actions, 1 KB simulate, 64 KB webhook, 2 MB uploads; a chunked 10 MB body stops at 413); scripts/check-env.mjs run by npm start refuses live Stripe keys, bad webhook secrets, secrets in NEXT_PUBLIC_*, bad spending caps and http URLs under HTTPS_ONLY, and warns about demo/admin switcher on public hosts; create-admin no longer prints the email; git history scanned (no keys, tokens, .env or database files ever committed); zero dangerouslySetInnerHTML/innerHTML/eval; every route checked at 390px with axe (fixed: 3D stage aria-label needed a role, scrollable tables now focusable labelled regions, admin application list overflow, consent checkbox layout in forms); quest page gets the shared error state; partner landing copy now says the partner receives 30% (matches the dashboard formula); HTTP smoke with DEMO_MODE=false (identities [], demo login 403, simulate 404, cross-site POST 403, SQL-injection search returns nothing, all security headers present); docs/ROUTE_STATES.md; README rewritten (security table, real vs simulated, LEGAL REVIEW REQUIRED, env table, business assumptions). M8 creator marketplace — migration 8 (otp_codes, listings, listing_characters with remaining ≤ declared CHECK, listing_photos, market_orders with buyer≠seller and subtotal = fee + owed CHECKs, append-only c2c_draws, chat_threads, chat_messages, reports) + seeded Mei/Jun/Priya listings and a PAID_HELD Alex←Mei order with a 3-message chat; src/domain fee.ts (8%, min S$0.50), draw.ts (stock-weighted), trust.ts; /me/verify simulated email + phone OTP (hashed, 10 min, 5 tries, code shown only in DEMO); /sell dashboard (trust, held, owed, platform fee), /sell/new listing editor (2–12 characters, declared stock, 1–6 photos via POST /api/upload with magic-byte sniffing ≤2 MB, served by /api/uploads/[id] with nosniff + sandbox CSP); /market grid with search, theme, price and rarity filters; /market/[id] stock and odds table, quantity 1–10, age checkbox, Buy boxes (draws inside one BEGIN IMMEDIATE txn, Stripe C2C session or simulated payment); /orders buying/selling tabs; /orders/[id] buyer reveal (1.2 s per box, Skip), seller pick list and Mark as fulfilled, Confirm received as drawn, Report a problem; admin reports Uphold (refund, payout void, trust recomputed, <50 suspends listings) / Dismiss; sweep auto-completes orders fulfilled >7 days ago; /orders/[id]/chat (parties only, 5 s polling, 1000 chars, 30 per 10 min, text-only rendering, fixed off-platform payment banner). Tests: 25 marketplace service tests incl. 50 parallel buys over 5 connections for 5 boxes, seller cannot buy own (403 + DB CHECK), IDOR, draw log append-only, chat <script> rendered as text; e2e Alex buys 2 from Mei → reveal → Mei pick list → fulfil → Alex confirms → chat → Mei sees S$22.08 owed and S$3.84 fees; e2e verification + new listing with photo (fake PNG refused). M7 partner portal — accounts (sign up / sign in / sign out, scrypt, hashed session tokens, lockout, npm run create-admin); B2C engine campaign-aware (Astral default; home lists More drops; drop/quest/checkout take ?campaign=); migration 7 partner_applications + seeded Kopi Kaki Collective draft and Hawker Heroes application; /partners landing + application form (Brand or Collective, IP statement required); admin approves / rejects / requests info; /partner dashboard (plays, wins, win rate, paid, sell-through, trades, revenue, 30% share, manifest); /partner/campaign/[id] editor (2–8 characters, units must equal cap, locked after submit/publish); e2e: new account applies → admin approves → partner drafts and submits → admin publishes → drop appears on home. Also: security headers (CSP, HSTS on HTTPS), rate limits on every API route, Render Blueprint + Dockerfile. M6 admin — migration 5 (users rebuilt with ADMIN role + email/phone/trust columns, partners + partner_members with Astral Studio backfilled, campaigns rebuilt with DRAFT/IN_REVIEW/CANCELLED phases, partner_id, revenue_share_bps); Admin demo identity (gated by ADMIN_DEMO); /me lists demo identities; admin console in /studio: campaigns table with Publish (builds pool + fingerprint), Close preorder (reveals seed, second call 409), advance, manifest download, Sweep (expire + re-match), audit trail filter; GET /api/manifest CSV (partner member or ADMIN, formula-safe cells); e2e golden path downloads the manifest and checks the total row. M5 Stripe — stripe 22.6.2 SDK; migration 4 (orders status CHECK via rebuild keeping 7 columns, capacity guard counts only PENDING_PAYMENT/PAID/DEMO_PAID, payments, webhook_events, audit_log append-only, access.order_id, users.age_confirmed_at); checkout action (age required) reserves a seat then creates a Checkout Session (31-minute expiry, amount from DB, live keys refused, MAX_CHARGE_CENTS cap); POST /api/stripe/webhook (raw body signature check, dedupe, paid → allocate, expired → release, late payment → revive or refund); POST /api/stripe/simulate (DEMO only); /checkout/success polls every 1.5 s up to 60 s; cancel link frees the seat; lazy expiry on read; e2e golden path step now clicks "Pay with card". M4 fairness — src/domain/fairness.ts (buildPool, HMAC-SHA256 Fisher–Yates shuffle, canonical, commitment, verify) + Web Crypto twin; migration 3 (characters.units/color/description, pool_units, fairness_commitments, allocations.pool_unit_id unique, append-only triggers; v1 demo data is replaced automatically, real orders would require reset:demo); fixed disclosed DEMO_SEED_HEX via scripts/find-demo-seed.mjs; allocation = lowest unsold position; seed revealed at PREORDER_CLOSED; GET /api/verify; /verify/[campaign] recomputes in the browser and shows the green bar; reveal shows "Box 94 of 100". M3 studio + attempts — forward-only migration runner (PRAGMA user_version), campaigns.required_score/attempts_per_day/game_mode, game_sessions.won; daily attempt limit per SGT day (429 ATTEMPT_LIMIT, 50 in demo), tries-left line and limit state on /quest, demoWin action + "Demo: win instantly" (DEMO only, else 404); studio restyled with Plays/Wins/Win rate/Confirmed/Sell-through/Trades stats, scrollable table, phase timeline; edit dialog adds stars-to-win and tries-per-day; mobile pass (no overflow, all tap targets ≥44px). M2 collector screens — home (Frame 1: live badge, box stage + claimed stamp, meter, 5-step how-it-works), drop (Frame 2: lineup with tier and N of 100 boxes, Secret kin silhouette, sticky price panel), quest (dark arena, Unbounded HUD), checkout (price ticket, simulated-payment note), reveal (≤2.4 s shake → split → foil burst → name; revisit/reduced motion = final frame), collection, trades (Frame 4 facing cards + swap disc); loading/empty/error states via Pending, Empty, ErrorNote. M1 design system and shell — Unbounded + Figtree fonts, Night Counter tokens (legacy variables re-pointed), no text below 13px, no caps eyebrows in CSS, ui/ primitives (Button, Tier, Stat, Card, Empty, ErrorNote, Skeleton, Perforation), new shell (logo, pill nav with aria-current, account chip, mobile menu, bottom tab bar), skeleton loading, recoloured characters, /market and /partners stubs, /me account hub, /terms draft, app icon. M0 baseline and safety net — baseline-v1 tag, all checks green before changes (typecheck, lint, 23 unit, build, 5 e2e); .env.example lists Stripe/demo variables; `npm run reset:demo`; `GET /api/health`; `npm start` binds to HOSTNAME (default 127.0.0.1); README "Deploy (single host)"; Playwright can use a local Chromium via PW_CHROMIUM_PATH.
IN PROGRESS: nothing
BLOCKED: nothing
BUGS: none recorded yet.
DECISIONS: demo sign-in with DEMO_MODE=false stays 403 DEMO_DISABLED (pinned by an M6 test; identities are hidden, which meets the M9 criterion); /api/health is not rate limited so the host's health check can never be throttled; marketplace trust uses the formula exactly (100 − 25×upheld + 2×completed); seeded sellers start from their imported score until their first recompute; C2C buyers need not be verified (only sellers), per the API table; partners/partner_members arrive in M6 (not M7) because manifest access is "partner member or ADMIN"; partner owners may still advance their own campaign's phases because the protected e2e golden path does so, while publish, close and sweep are ADMIN only; orders keep their 7 columns (a unit test inserts positionally), so the Stripe session id and paid time live in payments; the legacy `preorder` action stays as a DEMO-only reserve + simulated payment because the e2e API test calls it; keep SQLite + single host (no Supabase/Vercel); in-app payment only for C2C; fixed disclosed demo seed; CAPTCHA moved to P2; Codex used as read-only reviewer. Run upper time bound stays at 1 hour (spec says duration+20 s) because the protected pause feature would otherwise void paused runs. Playwright keeps `channel: 'chrome'` by default; PW_CHROMIUM_PATH overrides it for machines without Chrome.
NEXT 3 ACTIONS: 1) open the Render deploy link in README and click Apply  2) rehearse DEMO.md on the live URL and record docs/demo.mp4  3) before leaving the site public: ADMIN_DEMO=false and npm run create-admin
LAST GREEN TAG: m9-green
